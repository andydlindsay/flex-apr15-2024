"use strict";
// if it looks like a duck, and walks like a duck, and quacks like a duck, then it must be a duck
const login = (user) => {
    return true;
};
const myUser = {
    username: 'alice',
    password: '1234',
    anotherKey: 'hello'
};
login(myUser);
